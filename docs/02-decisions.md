# 決定記録
形式：日付 / 何を / どう決めたか / 理由 / 暫定or確定

2026-07-20 / トライの座標計算(computeTargets)の置き場所 / core/layout.jsが無いので src/views/layout.js に純関数として実装 / core/を無断で書き換えない取り決め（CLAUDE.md）。spec §4.1では本来core領域 / 暫定（core/への追加要望としてPRで報告）

2026-07-20 / ノードの同一性 / M1プロトタイプの文字列連結ではなく、core/trie.jsのpathKey（原子配列をU+241Fで結合）をSVGノードのReact keyおよびバネ状態Mapのキーに採用 / spec §9-A2の多重字衝突を避けるため（tableトークナイザ語彙で必須） / 確定

2026-07-20 / GitHub Actions → Pages 配備 / ワークフローYAML案を docs/deploy-pages.yml に用意したが、このセッションのGitHub App権限では .github/workflows/ への書き込みができないため、PR説明で発注者に手動追加を依頼する / claude-code-actionの制約 / 暫定（発注者が追加すればM1完了条件を満たす）

2026-07-20 / 単一HTML書き出し先 / vite.config.jsのbuild.outDirを直接release/にし、vite-plugin-singlefileで常に単一HTMLとして出す（distとreleaseを分けない） / 「配布用に別途コピーする」手順を増やさないため / 暫定（触感で問題なければ確定）

2026-07-20 / 語彙エディタでの新規語の詠み方 / セグメント(seg)は素通しで保存するが、フォーム追加時はw・gのみを書く（seg・idは編集しない） / spec §3.1「必須はwのみ」。辞書UIも作らない方針のため形態素分割は編集対象にしない / 暫定

2026-07-20 / エントリid採番 / 既存4桁ゼロ埋め連番(0001…)の最大値+1を採番。旧M1配列インポートも同形式に変換 / data/vocab/en-b2-verbs.jsonの既存採番方式に合わせる / 暫定

2026-07-20 / 詠み切り札の表示 / kimarijiDepthがnullの語は詳細シートで「詠み切り札」バッジ（青バッジの代わりに灰色）を出す。M1プロトタイプにはこの分岐が無かった / spec §9-A1・core/kimariji.jsの仕様どおり実装 / 確定

2026-07-20 / PWA（manifest/service worker） / このPRでは作らない / Issueの「やること」4項目に含まれず、spec §6ではM5相当。CLAUDE.mdの「プランS範囲外の先行実装禁止」に該当すると判断 / 暫定（次PRで着手）

2026-07-20 / `node core/selftest.js` の実行確認 / このセッションのBash権限では `node core/selftest.js ./data` と `npm` コマンドが承認待ちで実行できなかった（`git status`/`node --version`等の読み取り専用コマンドのみ許可）。core/には一切手を加えていないため回帰は無いはずだが、機械的な確認はできていない / claude-code-actionの許可リスト制約 / 暫定（発注者に手元での実行を依頼。将来のPRのため許可リスト拡大を検討）

2026-07-20 / S-2: 語頭（focus=ε）の縦一列レイアウト / src/views/layout.js の computeTargets に focusAtoms.length===0 専用の分岐を追加。定数 COL_X=58 / COL_TOP=62 / COL_ROW=52 は触感で未調整の仮値。子は sortedKids（count降順）の順にそのままY方向へ並べる＝「頻度順に上から」を満たす / spec §5.2・§7-4「語頭だけ縦一列」。盤（扇）とは別のジオメトリなので既存のR_FAN/DTH等は一切変更していない / 暫定（行間・COL_Xは実機で調整）

2026-07-20 / 縦一列モードでの辺（edge）省略 / focus=εのときは全ての辺が根の同一点から出て縦方向に重なるだけで視覚的な情報が無いため、TrieView.jsx で isColumn 時は辺を描画しない / 単純に太い1本の縦棒が見えるだけになり、frequency=半径・countラベルで十分情報が足りるため。仕様に明記はないため裁量判断 / 暫定

2026-07-20 / 縦一列のスクロール実装 / 専用のスクロールUIを作らず、TrieView.jsx のキャンバスdivを focus=ε のときだけ overflowY:auto・touchAction:pan-y にし、svgの高さを内容量（columnHeight）に応じて伸ばすことでネイティブスクロールに任せた / 「スクロール可」の要求を満たしつつ実装を最小に保つため（盤モードの独自ドラッグ回転ジェスチャーと衝突しないよう分岐） / 確定

2026-07-20 / 魚眼トレイルの文字サイズ下限 / TrieView.jsx のノード文字描画から `r>=7` の非表示条件を外し、フォントサイズを全ノード種別で `Math.max(10, r*0.95)` に統一（旧: `Math.max(9,...)` かつ r<7で文字非表示）。円が魚眼で点になっても文字が消えないようにした / spec §5.2 P1「文字サイズに下限10px、円からはみ出してよい。奥は円が点になっても文字列は読める」。トレイル以外のkindにも同じ規則を適用したのは、実装を単純に保つため（focus/childは元々r>=13程度なので実質影響なし） / 確定

2026-07-20 / 決まり字の尾から単語・意味の直書きを削除 / TrieView.jsx の KimarijiTail から単語テキストとgloss表示を削除し、残りのゴースト文字連鎖のみに戻した（決まり字タップ時はDetailSheetが既に単語とglossを表示していたが、キャンバス側にも重複して直書きされていた） / spec §5.2「尾は小さなゴースト文字連鎖のみ、終端に単語テキストは置かない（語は下部シートで）」・§7-10。既存実装がこの決定に反していたバグとして修正 / 確定
2026-07-20 / トレイルの表現 / 魚眼（縮小＋間隔圧縮）を廃止し、等サイズ・等間隔の横一直線に変更。辿りは全ノードを保持し、溢れたら左へ横スクロール（古い側がフェードして画面外）。焦点だけ一回り大きく朱の輪。 / 理由：ノードの縮小と間隔の伸縮が視認性を損なうという実機フィードバック。全ノード保持はPC幅で溢れにくくレスポンシブと整合。 / 確定
2026-07-20 / 決定記録§7-3の改訂 / 「トレイル＝実木の魚眼ズーム、折れの向き＝兄弟内順位」を撤回。折れも魚眼も廃止し横一直線に。ただし「辿った道を実ノードとして残す」原則は維持。 / 理由：同上。魚眼の美観より可読性を優先。 / 確定
2026-07-20 / 下部シート常時表示 / トレイルが省略・スクロールされても、画面下部に現在の綴り全体を常時表示（辿った字＝濃、これから決まる字＝薄、決まり字到達時は語＋gloss＋n字決まり）。 / 理由：盤上で全綴りが読めなくても綴りの全体像を保証するため。 / 確定
2026-07-20 / S-3実装：姉妹ゴースト（トレイル脇の未選択枝プレビュー）の扱い / 魚眼スケール（sc）に依存していた機能のため、魚眼廃止と同時に削除。トレイルノードのタップ＝巻き戻りのみ残す / Issue本文に姉妹ゴーストの扱いの指示は無く、魚眼と一体だった仕組みを等サイズ・等間隔・直線という新要件にそのまま持ち込むと「横一直線」の見た目を壊すため / 暫定（発注者が復活を望むなら次PRで別仕様として再設計）
2026-07-20 / S-3実装：焦点の右寄り固定と横スクロールの具体化 / src/views/layout.js のcomputeTargetsでtrail/focusのxを `焦点=Math.min(dims.w*0.34,150)+STEP*depth`・`祖先=焦点-STEP*k`・`y=ay一定` に統一（GAP・S_TRAIL・折れオフセットを削除）。TrieView.jsx はcanvasのoverflowXをauto化し、layoutが変わるたびscrollLeftを`contentWidth-dims.w`に合わせて右端（焦点側）へスナップさせ、左端に紙色→透明のグラデーションを重ねて「古い側フェード」を表現 / 焦点のスクリーン上位置を厳密に固定するには負の座標系かCSS transformが必要になり複雑化するため、「深さが浅いうちは詰まって見え、溢れて初めて右固定が効く」という近似で妥協 / 暫定（触感で違和感があれば発注者にフィードバックを依頼）
2026-07-20 / トレイルノードの固定半径 / FE.TRAIL_R=15を新設（焦点FOCUS_R=19より一回り小さい固定値）。祖先ノードは根も含めて全て同じ半径にした（旧実装は根だけ11とやや小さくしていた） / spec「等サイズ」の要求を素直に満たすため、根だけ例外にする理由が無いと判断 / 暫定
2026-07-20 / 下部シート常時表示の実装場所 / 新規コンポーネントを作らず、既存DetailSheet.jsxにword=null分岐を追加してApp.jsx側の条件レンダリング（state.selection &&）を外し常時マウントに変更。ヘッダー下の「接頭：」表示は下部シートと重複するため削除 / 「常時表示」の一枚岩要求に対し、語選択時と未選択時で見た目が大きく違う2つのコンポーネントを両方常設するよりは1つのシートが状態で切り替わる方が単純 / 暫定
